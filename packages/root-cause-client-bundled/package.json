{
    "name": "@testim/root-cause-client-bundled",
    "version": "0.1.17",
    "private": false,
    "author": "testim.io <dev@testim.io>",
    "main": "main",
    "scripts": {
        "build": "rm -fr ./packageToPublish && yarn workspace @testim/root-cause-viewer-client build-craco-lib && mkdir -p ./packageToPublish && cp -r ../client/packageToPublish ./",
        "prepublishOnly": "./scripts/prepublishOnly.sh"
    },
    "license": "AGPL-3.0-only",
    "peerDependencies": {
        "mobx": "^5.15.4",
        "mobx-react-lite": "^2.0.7",
        "mobx-utils": "^5.6.1"
    },
    "devDependencies": {
        "@testim/root-cause-viewer-client": "^0.1.17"
    },
    "publishConfig": {
        "directory": "packageToPublish",
        "access": "public"
    }
}
